import { GeneralInfoInspection } from './general-info-inspection';

describe('GeneralInfoInspection', () => {
  it('should create an instance', () => {
    expect(new GeneralInfoInspection()).toBeTruthy();
  });
});

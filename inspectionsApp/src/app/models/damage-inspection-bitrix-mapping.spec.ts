import { DamageInspectionBitrixMapping } from './damage-inspection-bitrix-mapping';

describe('DamageInspectionBitrixMapping', () => {
  it('should create an instance', () => {
    expect(new DamageInspectionBitrixMapping()).toBeTruthy();
  });
});

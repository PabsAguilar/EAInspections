import { MoistureMappingAreas } from './moisture-mapping-areas';

describe('MoistureMappingAreas', () => {
  it('should create an instance', () => {
    expect(new MoistureMappingAreas()).toBeTruthy();
  });
});
